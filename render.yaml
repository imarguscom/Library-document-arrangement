services:
  - type: web
    name: library-document-arrangement
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run web_app.py --server.address 0.0.0.0 --server.port $PORT --server.headless true
